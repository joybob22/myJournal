import { StripTagsAndSlicePipe } from './strip-tags-and-slice.pipe';

describe('StripTagsAndSlicePipe', () => {
  it('create an instance', () => {
    const pipe = new StripTagsAndSlicePipe();
    expect(pipe).toBeTruthy();
  });
});
